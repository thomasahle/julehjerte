import{Cubic}from'../../static/inverse/core/bezier.js';import{reindex}from'../../static/inverse/core/graph.js';
export function fixture(name){const w=60,A=[],B=[];if(name==='straight'){for(const x of[20,40])A.push([[x,0],[x,60]]);for(const y of[20,40])B.push([[0,y],[60,y]]);}else if(name==='hidden-corner'){A.push([[20,0],[20,20],[40,20],[40,40],[40,60]]);B.push([[0,20],[20,20],[40,20],[40,40],[60,40]]);}else if(name==='neck')A.push([[20,0],[20,25],[35,25],[35,15],[50,15],[50,45],[35,45],[35,27],[20,27],[20,60]]);else if(name==='thin'){A.push([[20,0],[20,60]],[[21,0],[21,60]]);B.push([[0,20],[60,20]],[[0,40],[60,40]]);}else throw new Error('Unknown fixture');const lookup=new Map(),edges=[],uses=[];const paths=[A,B].map(f=>f.map(p=>p.slice(1).map((b,i)=>{const a=p[i],key=JSON.stringify([a,b]),rev=JSON.stringify([b,a]);let id,forward=true;if(lookup.has(key))id=lookup.get(key);else if(lookup.has(rev)){id=lookup.get(rev);forward=false;}else{id=edges.length;lookup.set(key,id);edges.push({curve:Cubic.line(a,b),visible:true});uses[id]=0;}uses[id]++;return[id,forward];})));edges.forEach((e,i)=>e.visible=uses[i]===1);const target={width:w,phase:0,curves:edges.filter(e=>e.visible).map(e=>e.curve),metadata:{input:'controlled test fixture',paperColors:['#bd1111','#ffffff']}},graph=reindex({target,edges,metadata:{fixture:name}});return{graph,paths,report:{fixture:name}};}
export function checkerPixels(n=64,cells=4){const rgba=new Uint8ClampedArray(n*n*4);for(let y=0;y<n;y++)for(let x=0;x<n;x++){const i=4*(y*n+x),v=(Math.floor(x*cells/n)+Math.floor(y*cells/n))%2;rgba.set(v?[190,20,20,255]:[255,255,255,255],i);}return{type:'pixels',rgba,imageWidth:n,imageHeight:n};}
